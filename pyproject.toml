[project]
name = "notifygate"
version = "0.1.0"
description = "Universal notification filter for AI agents"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic-settings>=2.0",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
